use crate::{interfaces::ExtractConstraintsInterface, primitives::{Circuit, Constraints}};


impl ExtractConstraintsInterface for Circuit {
    fn extract_constraints(&self) -> Constraints {
        todo!()
    }
}